# ================================
# Required Environment Variables
# ================================

# Discord bot token (required)
# Get from: https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# ================================
# Optional Environment Variables
# ================================

# Database URL (optional - defaults to SQLite if not set)
# Local development uses SQLite by default (no configuration needed)
DATABASE_URL=sqlite://data/database.db

# Debug token for local testing with --debug flag (optional)
# Allows running a separate bot instance for development
DEBUG_TOKEN=your_debug_bot_token_here

# Environment indicator (optional - not currently used by code)
ENVIRONMENT=dev

